name: coinbase_cloud_exchange_dart_client
description: Coinbase Cloud Exchange Dart Client for building applications that interact with the coinbase cloud exchange & Advanced Trade api's
version: 0.1.1

environment:
  sdk: '>=2.17.3 <3.0.0'

dependencies:
  crypto: ^3.0.2
  http: ^0.13.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
